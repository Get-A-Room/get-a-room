describe('adminAPI', () => {
    describe('getBuildingData', () => {
        test.todo('Should throw error if buildings could not be get');
        test.todo('Should return buildings');
    });

    describe('getRoomData', () => {
        test.todo('Should throw error if rooms could not be get');
        test.todo('Should return rooms');
    });
});
